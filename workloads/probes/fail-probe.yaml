experiment_name: air-lab-fail-probe
environment:
  version: "5"   # v4 job-plane egress broken on hardened ws — see docs/06
  dependencies: []
compute:
  num_accelerators: 1
  accelerator_type: GPU_1xA10
command: |
  echo "deliberate failure probe"
  exit 1
max_retries: 0
timeout_minutes: 15
mlflow_run_name: fail-probe
