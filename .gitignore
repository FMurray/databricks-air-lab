workloads/*.yaml
!workloads/*.example.yaml
docs/private/
# numbered cookbook docs (01-06) — local-only, not for showing
docs/0[1-6]-*.md
apps/training-hub/config/teams.yaml
.DS_Store
.venv/
.obsidian/
__pycache__/
experiments/docker-otel-zerobus/vendor/
.claude/skills/*-workspace/
.mkdocs-site/
experiments/**/run-*.log
experiments/**/preflight-*.log
!workloads/probes/
# vendored binary wheels stay out of git (deployed to the workspace mirror instead;
# also keeps them out of air-CLI snapshot tars, which respect .gitignore)
*.whl
workloads/**/.composed-*.yaml
